/*global beforeEach, describe, it, assert, expect  */
'use strict';

describe('Basket View', function () {

    beforeEach(function () {

        this.BasketView = new Menu.Views.Basket();
    });

  it('correctly adds '  + ' args', function() {
      assert.equal(0,1);
  });

});
